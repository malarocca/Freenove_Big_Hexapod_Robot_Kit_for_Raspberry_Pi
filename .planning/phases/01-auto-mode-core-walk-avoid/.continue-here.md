---
context: phase
phase: 01-auto-mode-core-walk-avoid
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-08-08T23:06:00.000Z
---

<latest_blocker>
BLOCKED ON PHYSICAL HARDWARE, not on any code/agent work. Two real code bugs were found and fixed live this session (commits ff87cda, 12a7fda — see below and HANDOFF.json for full detail), but after both fixes the ultrasonic sensor reads a constant 300.0cm (max-range clamp) regardless of what's in front of it, even a hand at 2-5cm. Confirmed via BOTH the new autonomy code AND the completely untouched, pre-existing `Code/Server/ultrasonic.py` self-test (predates this session, is what the existing CMD_SONIC manual-teleop feature already relies on) — same stuck-at-300 result either way. This rules out software. It's a physical sensor/wiring/power fault, likely PRE-EXISTING (not introduced by this phase). User is powering down to inspect wiring; this checkpoint was written just before that. On resume: see `next_action` in HANDOFF.json — do NOT redo the hysteresis decision (already answered: fix now, both fixes committed and verified correct by design), just get the physical sensor working again, starting with the simplest possible test (`sudo python3 ultrasonic.py`) before layering back up to the full walkthrough.
</latest_blocker>

<current_state>
Running `/bm:execute-phase 01` (Phase 01: Auto-Mode Core — Walk & Avoid, 3 plans / 3 sequential waves, all `autonomous: false`).

Wave 1 (plan 01-01, "Walking Skeleton") executor completed Tasks 1-2 in an isolated
git worktree, then stopped at Task 3's `checkpoint:human-verify` gate. The user is
now LIVE, mid-walkthrough, on real hardware (session date 2026-08-08). Robot + Pi
are running on battery, so this file is being kept up to date mid-checkpoint (not
just at session pause) in case of a sudden power loss — see `.planning/HANDOFF.json`
`checkpoint_progress` for the authoritative step-by-step state.

Progress so far: steps 1, 2, 3, 6 PASSED (with a fix needed for step 1 and a minor
double-ack noted in step 3). Step 4 (obstacle stop) FAILED in a specific way: the
robot repeatedly stopped-and-retried against a held box and made physical contact
with it. Root cause is understood (see below) and a decision on how to proceed is
PENDING from the user — do not silently pick an option.
</current_state>

## Critical Anti-Patterns

_None from Tasks 1-2 (that part went cleanly). One live finding from the Task 3 checkpoint itself, not yet resolved:_

- **Obstacle contact during step 4 of the walkthrough.** `autonomy/behavior.py`'s `decide()` is a stateless single-20cm-threshold check with no hysteresis, so a single noisy ultrasonic reading just above 20cm reads CLEAR and triggers a step even with an obstacle still present. `settings.py` already defines `RESUME_THRESHOLD_CM = 35.0` ("D-06, hysteresis resume gate") but it is never read anywhere — dead config. This is exactly `01-01-PLAN.md` line 201's own documented plan ("plan 03 replaces this with the three-state hysteresis machine") — so the code matches what plan 01-01 asked for — but CLAUDE.md's stated non-negotiable core value is "the hexapod can move around on its own without crashing into things, pets, or kids," and contact occurred. **Do not treat this checkpoint as auto-approved.** The user was asked (via AskUserQuestion) to choose: (A) fix now — wire `RESUME_THRESHOLD_CM` into a stateful `decide()`/`AutoModeController` via a continuation agent, then redo step 4; or (B) accept as the documented Pitfall-2 gap and finish the remaining steps as-is, noting it prominently in `SUMMARY.md`, deferring the real fix to plan 01-03. **No answer was given yet** — the user redirected to a battery/power-loss durability concern instead. Re-ask this on resume if it's still unanswered.

<completed_work>

Completed Tasks (plan 01-01):
- Task 1: Create the autonomy package (`Code/Server/autonomy/`: `__init__.py`, `settings.py`, `perception.py`, `behavior.py`) — Done, commit `cdf822c` on branch `worktree-agent-ab149f1102043bce0`
- Task 2: Wire `CMD_AUTO` into `command.py`/`server.py`/`control.py`/`main.py`, make `run_gait` manually interruptible (~10ms override latency vs ~1.7s before), fix the dead TCP reconnect path (D-01) — Done, commit `ba4efbb` on the same branch
- Task 3: On-device walkthrough — In progress, LIVE as of 2026-08-08. Steps 1 (needed a fix), 2, 3, 6 PASSED. Step 4 FAILED (obstacle contact — see Critical Anti-Patterns above). Steps 5, 7, 8, 9 not yet run. Robot currently stopped/safe. Blocked on user's fix-now-vs-defer decision, not on a fresh human-verify gate.
</completed_work>

<remaining_work>

- Task 3 (plan 01-01): Get the user's fix-now-vs-defer decision on the step-4 obstacle-contact finding (see Critical Anti-Patterns / HANDOFF.json `pending_decision`). If fix-now: spawn a **continuation** executor agent (not a resume) into the existing worktree at `.claude/worktrees/agent-ab149f1102043bce0` (branch `worktree-agent-ab149f1102043bce0`, based on commits `cdf822c` + `ba4efbb`) with the deviation context, have it wire `RESUME_THRESHOLD_CM` into a stateful `decide()`, then redo step 4 live before continuing. Either way, still need to run steps 5, 7, 8, 9 (and re-verify step 4 if a fix is applied) before Task 3 can complete, write `SUMMARY.md`, and commit it.
- After plan 01-01's SUMMARY.md is committed, the orchestrator merges `worktree-agent-ab149f1102043bce0` back into `autonomy`, updates STATE.md/ROADMAP.md, removes the worktree, and proceeds to **Wave 2** (plan 01-02 — Auto Mode client GUI toggle + status badge).
- **Wave 3** (plan 01-03 — real avoidance: sweep, pick open side, turn, hysteresis) has not started; depends on 01-01 and 01-02. If the fix-now path is taken, plan 01-03 should still do the full sweep/turn/hysteresis machine — the pulled-forward fix is a minimal patch, not a substitute.
</remaining_work>

<decisions_made>

- No new decisions from Tasks 1-2 beyond what's already locked in `01-CONTEXT.md` (D-01 through D-10) — those were implemented as planned.
- Still PENDING (not yet decided): fix-now vs defer-to-01-03 for the step-4 obstacle-contact finding. See HANDOFF.json `pending_decision`. Do not assume an answer.
</decisions_made>

<blockers>
- Fix-now-vs-defer decision on the obstacle-contact finding — blocking, needs the user.
- Remaining walkthrough steps 5, 7, 8, 9 still need to be run live regardless of that decision.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` `checkpoint_progress` and `pending_decision` — the authoritative, most current state (more detailed than this file for today's live session).
2. `.planning/phases/01-auto-mode-core-walk-avoid/01-01-PLAN.md` — check its `must_haves.truths` and line 201 (explicitly scopes hysteresis out of this plan) against the step-4 finding.
3. `.planning/phases/01-auto-mode-core-walk-avoid/01-CONTEXT.md` — locked D-NN decisions, especially D-06 (hysteresis, Pitfall 2).

## Infrastructure State (as of 2026-08-08, mid-checkpoint)
- Server process IS running: `sudo python3 main.py -t -n` in Terminal A, from the WORKTREE dir `.claude/worktrees/agent-ab149f1102043bce0/Code/Server` (NOT the main repo dir — that's on branch `autonomy` without the CMD_AUTO code). Server IP 10.47.30.12. If the Pi rebooted since, `/home/pi/start.sh` will have auto-relaunched the OLD stale server from the main repo dir on boot — check `ps aux | grep main.py` and if it's running from the main repo path instead of the worktree path, kill it and relaunch from the worktree with sudo, same as the step-1 fix.
- Terminal B has a live Python REPL with an open socket `s` to port 5002 — needed for remaining steps; if that terminal died, just reconnect (this doubles as step 9's test).
- Git worktree `.claude/worktrees/agent-ab149f1102043bce0` (branch `worktree-agent-ab149f1102043bce0`) is UNMERGED — do not delete this worktree or branch before plan 01-01 is finished and merged. It is the only copy of commits `cdf822c` and `ba4efbb`.
- Main branch `autonomy` is at `88bccf8`.
- Robot is currently stopped/settled (manual override sent and acked) — safe at last check.
- Pre-existing untracked files, unrelated to this phase: `Code/Server/__pycache__/`, `Code/Server/params.json`.

## Checkpoint Details — progress so far (see HANDOFF.json checkpoint_progress for full detail)

**Type:** human-verify
**Plan:** 01-01 — Progress: 2/3 tasks complete, task 3 mid-walkthrough

1. Start server — PASSED after a fix (sudo + worktree dir + killing a stale boot server).
2. Initial CMD_AUTO#0 sync on connect — PASSED.
3. CMD_AUTO#1 → robot walks — PASSED (minor: server double-acks, cosmetic, not safety-relevant).
4. Hold box ~15cm → stop-and-settle, remove → resume, repeat twice — **FAILED**: robot flickered stop/walk and made contact with the box. Root cause: no hysteresis in `decide()` (see Critical Anti-Patterns). Fix-now-vs-defer decision PENDING.
5. Cup hand over ultrasonic (no echo) → must STOP — NOT YET RUN.
6. Manual override (`CMD_MOVE#1#0#0#8#0`) during auto-walk → stop within ~half second + unsolicited `CMD_AUTO#0` — PASSED. `CMD_BALANCE#1` variant not yet tried.
7. Malformed `CMD_AUTO` payloads rejected — NOT YET RUN.
8. 30s bounded-halt test (temporarily set `AUTO_RUN_SECONDS=30`, restore to 300 after) — NOT YET RUN.
9. Disconnect/reconnect sync — NOT YET RUN.

Also record: any soft/angled-obstacle sensing weirdness (feeds plan 01-03) — YES, see step 4; measured stop distance vs. 20cm setting — box was held at ~15cm, contact still occurred due to flicker, not a raw threshold miscalibration.

<context>
This is straightforward wave-by-wave phase execution — nothing unusual in approach. The only wrinkle is that every plan in this phase requires a live hardware checkpoint (by design), so expect the same pattern for plans 01-02 and 01-03. This particular checkpoint surfaced a real tension between plan 01-01's deliberate scoping (single-threshold stop, hysteresis explicitly deferred to 01-03 per the plan's own text) and CLAUDE.md's non-negotiable "never crash into things" safety bar — actual contact occurred during testing. That's being treated as a live decision point with the user, not silently waved through.
</context>

<next_action>
Re-ask the user (if not already answered later in this session): fix the obstacle-contact/hysteresis gap now via a continuation agent, or accept it as the documented Pitfall-2 gap and defer to plan 01-03? Then proceed accordingly (see remaining_work above). Before doing anything physical with the robot, confirm current power/connection state hasn't changed (Pi reboot, dead REPL socket, etc.) using the Infrastructure State notes above.
</next_action>
